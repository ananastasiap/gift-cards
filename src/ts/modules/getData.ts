// export const getData = (
//   cardImageSelector: string,
//   cardFromSelector: string,
//   cardToSelector: string,
//   cardMessageSelector: string
// ) => {
//   const cardImage = document.querySelector(cardImageSelector);
//   const cardFrom = document.querySelector(cardFromSelector);
//   const cardTo = document.querySelector(cardToSelector);
//   const cardMessage = document.querySelector(cardMessageSelector);
// };
